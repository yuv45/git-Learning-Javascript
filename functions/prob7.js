//Problem-7
Write code to find the absolute difference of two numbers

Sample Input-1 12,4
Sample Output-1 8
Sample Input-2 4,18
Sample Output-2 14
NOTE: It must consist of two functions 1. To find the difference 2.//
function difference(a,b)
{
  let difference=a-b;
  return difference;
}
let result=difference(12,4);
console.log(result);
function absolute_difference(a,b)
{
 let absolute =Math.abs(4-18);
  return absolute ;
}
let answer=absolute_difference(4,18);
console.log(answer)

